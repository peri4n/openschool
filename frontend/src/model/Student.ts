export class Student {
    id?: string;
    first?: string;
    last?: string;

    constructor(id?: string, firstName?: string, lastName?: string) {
        this.id = id
        this.first = firstName
        this.last = lastName
    }

}
